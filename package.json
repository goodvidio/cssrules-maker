{
  "name": "cssrules",
  "version": "0.0.11",
  "description": "Takes a css file as input, tokenizes it and generates CSS rules in a separate JavaScript file",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Goodvidio",
  "dependencies": {
    "postcss": "^3.0.4",
    "underscore": "~1.7.0"
  }
}
